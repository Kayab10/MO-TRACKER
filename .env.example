# Copy to .env.local (and add the same two vars in Vercel → Settings → Environment Variables).
# Leave these blank / unset to run the app in local-only mode (localStorage, no sharing).
#
# Supabase → Project Settings → API
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
